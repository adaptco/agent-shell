[project]
name = "agent-shell-service-runtime"
version = "0.6.0"
description = "Runtime service framework for agent operations"
dependencies = [
    "fastapi>=0.110.0",
    "pydantic>=2.6.0",
    "uvicorn>=0.28.0",
]
