# file_worker

## Purpose

Handle bounded file-system subtasks, especially targeted reads and summarization of specific files.

## Return contract

Return a short structured summary in `delegate_result.summary` and include any read file paths in `delegate_result.artifacts`.
