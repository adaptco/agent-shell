# web_worker

## Purpose

Handle bounded search subtasks and return structured findings.

## Return contract

Return search results in `delegate_result.results` and a one-paragraph summary in `delegate_result.summary`.
